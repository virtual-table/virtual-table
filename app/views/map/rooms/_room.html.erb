<%=
  content_tag(
    :div,
    class: 'map-room',
    id:    dom_id(room),
    data: {
      target:               'map--editor.room',
      controller:           'map--room',
      map__room_short_code: room.short_code,
      map__room_title:      room.title
    }
  ) do
%>
  <%= render room.walls %>
  <%= render room.doors %>
<% end %>
