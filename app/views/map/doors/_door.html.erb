<%=
  content_tag(
    :div,
    class: 'map-door',
    id:    dom_id(door),
    data: {
      target:                'map-editor.door',
      controller:            'map--door',
      map__door_origin:      door.origin.join(','),
      map__door_destination: door.destination.join(','),
      map__door_closed:      door.closed
    }
  ) do
%>
<% end %>