(function(modal) {
  modal.show('<%=
    escape_javascript(
      render(
        'form',
        map:        @map,
        background: @background
      )
    )
  %>')
})(window.VTT && window.VTT.modal)