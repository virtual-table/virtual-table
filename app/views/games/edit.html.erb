<%
title t('.page_title', game: @game.title)
%>
<section class="section">
  <div class="container">
    <h1 class="title"><%= t '.edit_game' %></h1>
    <%= render 'form', game: @game %>
  </div>
</section>
